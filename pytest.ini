[pytest]
# This repo contains many one-off local verification scripts prefixed with `test_` in the project root.
# Constrain pytest collection to the real unit/regression tests under ./tests.
testpaths = tests
python_files = test_*.py
addopts = -ra
